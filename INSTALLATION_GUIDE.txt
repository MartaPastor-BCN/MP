╔════════════════════════════════════════════════════════════════════════════╗
║                    🟢 DEAL ID AUDIT TOOL - STREAMLIT                       ║
║                            INSTALLATION & SETUP                            ║
╚════════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: VERIFY PYTHON IS INSTALLED                                         │
└────────────────────────────────────────────────────────────────────────────┘

Open PowerShell and type:
  $ python --version

Expected output: Python 3.8.0 or higher

If Python is NOT installed:
  1. Go to https://python.org/downloads
  2. Download Python 3.11 or latest
  3. Run installer (CHECK "Add Python to PATH")
  4. Restart PowerShell


┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: INSTALL DEPENDENCIES                                               │
└────────────────────────────────────────────────────────────────────────────┘

In PowerShell, navigate to your workspace:
  $ cd "C:\Users\pastormarta\Vscode"

Install required packages:
  $ pip install -r requirements.txt

Wait for installation to complete (should take 1-2 minutes)

Or use the automatic setup script:
  $ .\setup.ps1


┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: RUN THE APPLICATION                                                │
└────────────────────────────────────────────────────────────────────────────┘

In the same PowerShell window, type:
  $ streamlit run app.py

Expected output:
  ╔════════════════════════════════════════════════════════════════╗
  ║  Welcome to Streamlit. Check out our demo in your browser.    ║
  ║  Local URL:    http://localhost:8501                          ║
  ╚════════════════════════════════════════════════════════════════╝

The app will automatically open in your default browser!


┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: USING THE APPLICATION                                              │
└────────────────────────────────────────────────────────────────────────────┘

When the app opens, you'll see three tabs:

  ╔═══════════════════════════════════════════════════════════════╗
  ║  [Single Deal Audit]  [Batch Audit]  [History]               ║
  ╚═══════════════════════════════════════════════════════════════╝

TAB 1: SINGLE DEAL AUDIT
  ┌─────────────────────────────────────────────────────────────┐
  │ • Enter deal information in the form                        │
  │ • Checkboxes for KVPs, Devices, and Countries              │
  │ • Fill in Floor Price, Inventory, Performance               │
  │ • Click "🔍 Run Audit" button                              │
  │                                                              │
  │ RESULTS:                                                    │
  │ ✅ GREEN (HIGH)     = Deal ready for launch (90%+)         │
  │ ⚠️  YELLOW (MEDIUM) = Fix issues first (60-89%)            │
  │ ❌ RED (LOW)        = Major work needed (<60%)             │
  └─────────────────────────────────────────────────────────────┘

TAB 2: BATCH AUDIT
  ┌─────────────────────────────────────────────────────────────┐
  │ • Click "Choose file" to upload CSV                         │
  │ • Use sample_deals.csv as an example                        │
  │ • Click "🔍 Run Batch Audit"                               │
  │ • See results for all deals in table format                │
  │                                                              │
  │ CSV REQUIREMENTS:                                           │
  │ - First row must have headers                               │
  │ - Required columns: deal_id, status, etc.                   │
  │ - Use semicolons (;) to separate multiple values            │
  └─────────────────────────────────────────────────────────────┘

TAB 3: HISTORY
  ┌─────────────────────────────────────────────────────────────┐
  │ • All audits run in this session appear here                │
  │ • Click any deal to expand and see details                  │
  │ • Includes validation results and recommendations           │
  │ • History clears when you close the app                     │
  └─────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│ EXAMPLE: AUDITING YOUR FIRST DEAL                                          │
└────────────────────────────────────────────────────────────────────────────┘

1. App is running at http://localhost:8501
2. Click "Single Deal Audit" tab
3. Fill in the form:

   Deal ID:                     D-1001
   Deal Status:                 Active
   Buyer Seat ID:               BS-12345
   
   KVPs (check all three):
   ☑ msft_refresh
   ☑ brand_safety
   ☑ inventory_type
   
   Geo Countries:
   ☑ US
   ☑ CA
   
   Device Types:
   ☑ Mobile
   ☑ Desktop
   
   Audience Segments:           tech_enthusiasts, finance
   
   Deal List ID:                DL-9876
   Floor Price (CPM $):         5.50
   Creative Approved:           ☑ (checked)
   Inventory Strength:          Strong
   Historical Performance:      Good

4. Click "🔍 Run Audit"

5. See results:
   
   ┌──────────────────────────────────────────────────┐
   │  Deal ID D-1001: HIGH ✅                         │
   │                                                  │
   │  ████████████████████████████████████ 100%      │
   │                                                  │
   │  📋 Deal ready for launch                        │
   └──────────────────────────────────────────────────┘
   
   Validation Checks:
   ✅ Deal Status: Active
   ✅ Buyer Seat ID: Present
   ✅ KVPs: Valid
   ✅ Targeting: All Pass
   ✅ Deal List ID: Approved
   ✅ Floor Price: Competitive
   ✅ Creative Audit: Approved
   ✅ Inventory: Strong
   ✅ Historical Performance: Good


┌────────────────────────────────────────────────────────────────────────────┐
│ KEYBOARD SHORTCUTS                                                          │
└────────────────────────────────────────────────────────────────────────────┘

While app is running in PowerShell:
  • [r] = Rerun the script
  • [s] = Save session state
  • [c] = Clear cached data
  • [q] = Quit

In browser:
  • [R] = Refresh page
  • [Ctrl+Shift+I] = Open Developer Tools


┌────────────────────────────────────────────────────────────────────────────┐
│ FILE LOCATIONS & DESCRIPTIONS                                              │
└────────────────────────────────────────────────────────────────────────────┘

MAIN APPLICATION:
  app.py ..................... The Streamlit app (RUN THIS!)
  requirements.txt ........... Python dependencies
  setup.ps1 .................. PowerShell setup script
  setup.bat .................. Windows batch setup script

DOCUMENTATION:
  START_HERE.md .............. Quick start guide (READ FIRST!)
  STREAMLIT_GUIDE.md ......... Complete user manual
  ARCHITECTURE.md ............ Technical architecture
  README_STREAMLIT.md ........ Quick reference

SAMPLE DATA:
  sample_deals.csv ........... Example CSV for batch testing

DATA ANALYSIS:
  Untitled-1.ipynb ........... Jupyter notebook


┌────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                             │
└────────────────────────────────────────────────────────────────────────────┘

❌ ERROR: "python is not recognized"
   → Install Python from python.org
   → Make sure to check "Add Python to PATH" during install
   → Restart PowerShell

❌ ERROR: "streamlit is not recognized"
   → Run: pip install streamlit
   → Make sure you're in the correct directory

❌ ERROR: "Address already in use"
   → Another app is using port 8501
   → Streamlit will auto-use 8502, 8503, etc.
   → Check browser - app might already be running!

❌ ERROR: "Module not found"
   → Run: pip install -r requirements.txt
   → Wait for all packages to install

❌ App won't open in browser
   → Go to http://localhost:8501 manually
   → Check PowerShell output for error messages


┌────────────────────────────────────────────────────────────────────────────┐
│ STOPPING THE APPLICATION                                                    │
└────────────────────────────────────────────────────────────────────────────┘

In PowerShell window where app is running:
  Press: [Ctrl] + [C]

Or:
  Press: [q]


┌────────────────────────────────────────────────────────────────────────────┐
│ VALIDATION CHECKLIST                                                        │
└────────────────────────────────────────────────────────────────────────────┘

Before you run an audit, ensure:
  ☐ Deal ID is entered
  ☐ Deal Status is selected
  ☐ Buyer Seat ID is entered (or left empty)
  ☐ At least one KVP is checked
  ☐ At least one Geo country is checked
  ☐ At least one Device type is checked
  ☐ Floor Price is between $2-$15
  ☐ Deal List ID is entered (or left empty)
  ☐ Inventory Strength is selected
  ☐ Historical Performance is selected
  ☐ Creative Approved checkbox is set
  
Then click "🔍 Run Audit"


┌────────────────────────────────────────────────────────────────────────────┐
│ MARKET STANDARDS (Built-in)                                                │
└────────────────────────────────────────────────────────────────────────────┘

  CPM Price Range:              $2.00 - $15.00
  Required KVPs:                msft_refresh, brand_safety, inventory_type
  Valid Device Types:           mobile, desktop, tablet
  Valid Countries:              US, CA, GB, DE, FR, AU, JP, IN, BR, MX
  Maximum Segments:             5


┌────────────────────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                                   │
└────────────────────────────────────────────────────────────────────────────┘

  1. Install Python (if not already installed)
  2. Install dependencies: pip install -r requirements.txt
  3. Run app: streamlit run app.py
  4. Try Single Deal Audit with example values
  5. Try Batch Audit with sample_deals.csv
  6. Check Audit History to see results
  7. Read STREAMLIT_GUIDE.md for advanced features


╔════════════════════════════════════════════════════════════════════════════╗
║                         🎉 YOU'RE ALL SET UP!                             ║
║                                                                            ║
║  Your Deal ID Audit Tool is ready to use. Start with:                     ║
║                                                                            ║
║    $ streamlit run app.py                                                 ║
║                                                                            ║
║  Questions? Check START_HERE.md or STREAMLIT_GUIDE.md                     ║
╚════════════════════════════════════════════════════════════════════════════╝
